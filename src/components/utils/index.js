import { getSortedData } from "./getSortedData";
import { getSortByRatingData } from "./getSortByRating";
import { getFilteredData } from "./getFilteredData";

export {getSortedData, getSortByRatingData, getFilteredData}